# Tests

Add meaningful behavior tests as processing features arrive. Use invented data and temporary directories; never actual prayer records or working church files.

Priority cases: missing or ambiguous song references, canceled evening services, malformed dates, unsafe output paths, failed writes preserving approved outputs, changed inputs invalidating approval, and retries not duplicating history.

The current help/version scaffold needs only smoke checks. No behavior tests are included yet.
